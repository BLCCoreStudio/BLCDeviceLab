{
  "name": "blc-device-lab",
  "version": "0.1.0-alpha.1",
  "private": true,
  "type": "module",
  "description": "Cross-platform physical Android device workspace powered by external adb and scrcpy executables.",
  "scripts": {
    "check": "node --check src/cli.js && node --check src/core/adb.js && node --check src/core/scrcpy.js && node --check src/core/diagnostics.js",
    "test": "node --test",
    "doctor": "node src/cli.js doctor",
    "devices": "node src/cli.js devices"
  },
  "engines": {
    "node": ">=22"
  }
}
