# Third-party components

No third-party binaries are bundled in this Phase 0 scaffold.

Expected external runtime tools:

| Component | Purpose | Distribution status in Phase 0 |
| --- | --- | --- |
| adb / Android Platform Tools | Device discovery and control | External only |
| scrcpy | Screen/audio/control transport | External only |
| FFmpeg | Used by scrcpy builds | Not bundled by this repo |
| SDL | Used by scrcpy client | Not bundled by this repo |
| libusb | Optional/platform dependency used by scrcpy | Not bundled by this repo |

Before any bundled installer is published, replace this document with exact version, license, source URL, notices and hashes for each shipped artifact.
